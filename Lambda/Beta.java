package Lambda;

/**
 * The `Beta` class represents the beta symbol in the RPAL interpreter.
 * It is a subclass of the `Symbol` class.
 */
public class Beta extends Symbol {
    public Beta() {
        super("beta");
    }
}
