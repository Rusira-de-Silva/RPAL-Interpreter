package Lambda;

/**
 * The `Bop` class represents a binary operator in the RPAL interpreter.
 * It extends the `Rator` class and provides functionality for binary operations.
 */
public class Bop extends Rator {
    /**
     * Constructs a new `Bop` object with the specified data.
     *
     * @param data the data associated with the binary operator
     */
    public Bop(String data) {
        super(data);
    }
}

