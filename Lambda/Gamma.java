package Lambda;

/**
 * The `Gamma` class represents the symbol "gamma".
 * It is a subclass of the `Symbol` class.
 */
public class Gamma extends Symbol {
    public Gamma() {
        super("gamma");
    }
}
