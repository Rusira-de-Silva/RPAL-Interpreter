package Lambda;

/**
 * The `Bool` class represents a boolean value in the RPAL interpreter.
 * It extends the `Rand` class and provides a constructor to initialize the boolean value.
 */
public class Bool extends Rand {
    /**
     * Constructs a new `Bool` object with the specified boolean value.
     *
     * @param data the boolean value represented by this `Bool` object
     */
    public Bool(String data) {
        super(data);
    }
}
